import { StyleSheet, Text, View, SafeAreaView } from "react-native";
import React from "react";

const OnboardingPages = () => {
  return (
    <SafeAreaView>
      <Text>OnboardingPages</Text>
    </SafeAreaView>
  );
};

export default OnboardingPages;

const styles = StyleSheet.create({});
